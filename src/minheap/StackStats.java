package minheap;

import java.util.Stack;

import league.Tuple;

public class StackStats extends Generics<Stack<Tuple>> {

	public StackStats() {
		super(new Stack<Tuple>(), new Stack<Tuple>(), new Stack<Tuple>(), new Stack<Tuple>(), new Stack<Tuple>(),
				new Stack<Tuple>(), new Stack<Tuple>(), new Stack<Tuple>(), new Stack<Tuple>());
	}

}
